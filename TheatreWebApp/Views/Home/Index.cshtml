@using TheatreWebApp.Models.Home

@model List<IndexPlayViewModel>

@{
    ViewData["Title"] = "Home";
}


<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">
        <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
        <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
        <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
    </ol>
    <div class="carousel-inner">
        @for (int i = 0; i < Model.Count(); i++)
        {
            var play = Model[i];
            <div class="carousel-item @(i == 0 ? "active" : string.Empty)">
                <img class="d-block w-100" src="@play.ImageUrl" alt="@play.Name">
                <div class="carousel-caption d-none d-md-block">
                    <h5><a class="btn btn-primary" asp-controller="Plays" asp-action="Details" asp-route-playId="@play.Id">@play.Name</a></h5>
                    <p>@play.ShortDescription</p>
                </div>
            </div>
        }

    </div>
    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>



<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    <h2 class="display-4">To-Do List</h2>
    <ul>
        <li>Take care that decimal is converted to money in the database. Or something of the sort.</li>
        <li>See if we can add more data validations - eg. validate show dates on creation.</li>
        <li>Fix validaton error messages.</li>
        <li>More creative seat numbering, please.</li>
    </ul>
</div>


<div class="d-flex justify-content-center" style="width: 20%; margin: auto">
    <input class="btn btn-primary" type="submit" value="1">
    <input class="btn btn-primary" type="submit" value="2">
    <input class="btn btn-primary" type="submit" value="3">
    <input class="btn btn-primary" type="submit" value="4">
</div>